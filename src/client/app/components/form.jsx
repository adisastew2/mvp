import React from 'react';

function Form(props) {
  return (
    <div>
      <input placeholder="name"/>
      <input placeholder="message"/>
      <button>Submit</button>
    </div>
  );
}

export default Form;